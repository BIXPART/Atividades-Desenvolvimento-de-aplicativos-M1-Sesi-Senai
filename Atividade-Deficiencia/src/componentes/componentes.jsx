import { StatusBar } from 'expo-status-bar';
import { useState } from 'react';
import { StyleSheet, Text, View, Button, TextInput, ScrollView,Alert,Image } from 'react-native';

export default function SubTitulo(){
return <Text style={{fontSize:30, textAlign:'center',marginBottom:20}}>Formas de se comunicar com pessoas surdas</Text>
}